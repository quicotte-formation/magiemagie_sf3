<div class="row">
    <div class="col-sm-12">
        <div class="table-responsive">
            <table class="table">
                <thead>
                <th>NOM</th>
                <th>JOUEURS</th>
                <th>ACTION</th>
                </thead>
                {% for partie in parties %}
                <tr>
                    <td>Partie {{ partie.id }}</td>
                    <td>{{ partie.joueurs | length }}</td>
                    <td>
                        <a href="{{ path("rejoindre_partie", { idPartie: partie.id } ) }}">
                            <button>Rejoindre</button>
                        </a>
                        <a href="{{ path("demarrer_partie", {idPartie: partie.id }) }}">
                            <button>Démarrer</button>
                        </a>
                    </td>
                </tr>
                {% endfor %}
            </table>
        </div>
    </div>
</div>